import React from 'react';
import Popup from '../Popup.js';
import { shallow } from 'enzyme';


it.skip('should match the snapshot with all data passed correctly to the Icon', () => {
    expect(wrapper).toMatchSnapshot();
  })